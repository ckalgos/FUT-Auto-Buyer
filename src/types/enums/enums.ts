export enum EventTypeEnum {
  TAP = "TAP",
  CHANGE = "CHANGE",
}

export enum UTItemPile {
  ANY = 0,
  CLUB = 7,
  GIFT = 9,
  INBOX = 8,
  PURCHASED = 6,
  TRANSFER = 5,
}

export enum UINotificationTypeEnum {
  NEGATIVE = 2,
  NEUTRAL = 1,
  POSITIVE = 0,
}
